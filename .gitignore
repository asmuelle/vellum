# Xcode (project is generated by XcodeGen — never commit it)
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
build/
*.xcresult

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# Secrets / local config (none should ever be needed — see TOOLS.md)
.env
*.env
*.local.json

# Test artifacts
coverage/
*.profraw
*.profdata
